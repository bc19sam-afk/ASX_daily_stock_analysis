# AGENTS.md

## 仓库目标
当前默认定位是 ASX/AU/US 股票分析与报告辅助系统，服务证据整理、风险提示、仓位建议、模拟盘和人工复核。默认工作流是 human-in-the-loop：系统给计划和证据，用户自己决策/下单。

它不是默认自动交易系统，但也不禁止未来研究或实现澳洲券商接入、半自动确认或自动执行。只有任务明确进入 execution/broker scope 时，才展开这些边界。

## 开发原则
1. 优先保证报告口径一致、时间基准清楚、避免误导用户
2. 日常报告、数据源、UI、文档、测试改动不要反复加入"不做自动交易"声明；只在改动涉及券商、真实账户、订单执行、自动止损/止盈时说明执行边界
3. 涉及券商、真实账户或自动执行时，必须先明确模式边界：报告建议、模拟盘、手动记录、人工确认执行、全自动执行不能混淆
4. 真实账户执行必须默认关闭，并配套 dry-run/模拟盘、人工确认开关、审计日志、幂等保护、限额/熔断、凭证隔离、失败恢复和清晰免责声明
5. 可以研究和实现澳洲券商接入的抽象层、测试替身、只读行情/持仓读取、订单预检和模拟执行；接入真实下单 API 前必须单独评估时区/交易日、费用税务、最小订单、订单状态同步和安全风险
6. 改动尽量小，优先补测试，不做无关重构
7. 一个任务一个 PR

## ASX Wiki Protocol

新对话或新任务进入本仓库时，先把 `omx_wiki/` 当作项目长期记忆入口，而不是只依赖聊天上下文。

1. ASX 总控、规划、路线图、日常维护、上游对比、边界确认任务，先读 `omx_wiki/index.md` 和 `skills/asx-daily-stock-maintainer/SKILL.md`
2. 长期有效的项目结论、路线图、边界、runbook、架构摘要和外部约束，更新到 `omx_wiki/` 对应页面
3. 重复执行流程或 stop rule 的变化，更新 `skills/asx-daily-stock-maintainer/SKILL.md`
4. 只把压缩后的决策和证据索引写入 wiki，不把完整聊天记录、临时推理、长工具输出原样塞进 wiki
5. 不保存 API keys、broker credentials、HIN 原文、账户号码、真实下单细节、真实成交回执或任何可访问/操作账户的秘密
6. 如果用户明确说"只做总控/规划/记录"、"不写文件"或"不执行代码改动"，必须停在草案、分析和文件清单层面，除非用户之后明确授权执行
